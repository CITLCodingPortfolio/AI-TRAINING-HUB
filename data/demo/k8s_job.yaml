apiVersion: batch/v1
kind: Job
metadata:
  name: demo-batch-job
spec:
  template:
    spec:
      containers:
      - name: worker
        image: myorg/worker:latest
        args: ["--input","/data/input.csv"]
      restartPolicy: Never