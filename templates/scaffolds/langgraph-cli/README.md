# LangGraph Starter (CLI)
This scaffold is intentionally minimal. Replace with your LangGraph setup.
Default demo: run a CLI bot against sample files.
Example:
  python -m bots.run_cli --bot it_ticket_bot --input-file data/demo/sample_ticket.txt --file data/demo/policy.txt
